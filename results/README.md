# Results

RiskShard writes local CLI output here, including JSON reports from `--export` and Loss Exceedance Curve PNGs.

Routine generated outputs are ignored by Git. If a result becomes a curated example, move it to a documented examples location before committing it.
